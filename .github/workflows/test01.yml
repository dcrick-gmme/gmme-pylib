name: CI/CD test for building python package

on:
  workflow_dispatch: 
    branches: [ main ]

jobs:
    # build
    build:
        runs-on: windows-latest
    
        steps:
        - uses: actions/checkout@v4
        - name: Setup Python
          uses: actions/python
          with:
            python-version: '3.13'
                
