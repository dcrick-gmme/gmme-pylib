name: Testing github actions

on:
    workflow_dispatch: 
        branches: [main]

    push:
        branches: [main]

jobs:
    testing:
        runs-on: windows-latest
        steps:
            - run: echo "this is a test"